# Краткая схема облака

```text
GitHub → Streamlit Community Cloud → PostgreSQL (Supabase/Neon)
```

Streamlit хранит интерфейс и Python-код. PostgreSQL хранит общие данные всех подразделений и не теряет их при перезапуске приложения.
